# Puerto en el que se ejecuta el servidor
PORT=4000

# Cadena de conexion a MongoDB (local con MongoDB Community Server)
MONGODB_URI=mongodb://127.0.0.1:27017/proyectoDB

# Clave secreta usada para firmar los tokens JWT
JWT_SECRET=clave_secreta_zajuna_2026
